module Fourto1mux(in,select,y);
	input [3:0] in;
	input [1:0] select;
	output y;
	
	always @(*) begin
		case (select)  
			2'b00:y=in[0];	
			2'b01:y=in[1];
			2'b10:y=in[2];
			2'b11:y=in[3];	
		endcase
	end
	
endmodule
